import SwiftUI

struct SettingView: View {
    var body: some View {
        HStack {
            Text("설정")
                .font(.title2)
                .padding()
        }
    }
}

#Preview {
    MainView()
}
